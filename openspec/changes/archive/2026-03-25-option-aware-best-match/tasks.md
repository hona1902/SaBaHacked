# Tasks

## Task 1: Add `computeOptionSimilarity()` helper

Add a new function in `answer-bank.js` that computes how well a bank entry's options match the page options.

## Task 2: Update `findLocalAnswer()` to use combined scoring

After collecting all question matches, compute option similarity for each, combine scores, and re-sort to pick the true best match.

## Task 3: Include `optionSimilarityScore` in `allMatches` display data

So the UI can show which candidates had good option matches too.
